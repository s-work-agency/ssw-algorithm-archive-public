{
  "schemaVersion": "1.0",
  "vectorId": "bubble-sort-basic",
  "algorithmId": "bubble-sort",
  "cases": [
    { "caseId": "orders-unsorted-values", "description": "Sorts duplicate and negative integers.", "input": { "values": [3, -1, 3, 0] }, "expected": { "output": { "values": [-1, 0, 3, 3] } } },
    { "caseId": "accepts-empty-values", "description": "Empty input remains empty.", "input": { "values": [] }, "expected": { "output": { "values": [] } } },
    { "caseId": "keeps-single-value", "description": "원소가 하나인 배열은 이미 정렬된 기저 사례예요.", "input": { "values": [7] }, "expected": { "output": { "values": [7] } } }
  ]
}
